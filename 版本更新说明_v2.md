# 化妆品资讯系统 v2.0 更新说明

## 版本信息
- 版本号：v2.0
- 更新日期：2026-03-31
- 更新类型：重大功能调整

## 主要变更

### 1. 移除的功能模块
❌ 移除 **政策法规** 模块
❌ 移除 **标准规范** 模块

### 2. 新增和优化的功能模块

#### 📢 公告管理（新增）
- 支持上传抽样检查公告
- 支持附件上传（PDF、Word、Excel）
- **自动提取关键信息**：
  - 检验单位
  - 检验时间
  - 抽检批次数量
- 公告状态管理（草稿/已发布/已归档）
- 附件下载功能

#### 📊 抽样检查（优化）
- 与公告关联管理
- 支持企业关联
- 统计数据展示
- 检查详情查询

#### 🏢 企业管理（新增）
- 企业信息库
- 企业列表查询
- 企业详情查看
- **企业检查统计**：
  - 被抽检次数
  - 合格率统计
  - 不合格记录
- 筛选功能：
  - 按企业名称筛选
  - 按品牌筛选
  - 按省份筛选
  - 只看不合格企业

#### 📈 统计分析（新增）
- 不合格企业列表
- 不合格次数排名（TOP10）
- 省份分布统计
- 数据可视化图表
- 企业合格率分析

#### ⚠️ 督查结果（保留）
- 督查信息管理
- 整改状态跟踪

## 数据库变更

### 新增表
- `companies` - 企业信息表
- `company_inspection_stats` - 企业检查统计视图

### 修改表
- `announcements` - 公告表，增加以下字段：
  - `announcement_no` - 公告编号
  - `inspection_unit` - 检验单位（自动提取）
  - `inspection_count` - 抽检批次（自动提取）
  - `attachment_path` - 附件路径
  - `attachment_name` - 附件名称

- `inspection_details` - 检查详情表，增加字段：
  - `company_id` - 关联企业ID

## 技术栈更新

### 后端新增依赖
```json
{
  "multer": "^1.4.5-lts.1"  // 文件上传中间件
}
```

## API 接口变更

### 新增接口

#### 公告管理
- `POST /api/announcements` - 上传公告（支持附件）
- `PUT /api/announcements/:id` - 更新公告
- `DELETE /api/announcements/:id` - 删除公告

#### 企业管理
- `GET /api/companies` - 获取企业列表
- `GET /api/companies/:id` - 获取企业详情
- `GET /api/companies/stats/overview` - 获取企业统计
- `GET /api/companies/unqualified/list` - 获取不合格企业列表
- `POST /api/companies` - 创建企业
- `PUT /api/companies/:id` - 更新企业
- `DELETE /api/companies/:id` - 删除企业

### 移除接口
- ❌ `GET /api/policies/*` - 政策相关接口
- ❌ `GET /api/standards/*` - 标准相关接口
- ❌ `GET /api/policies/categories/list`
- ❌ `GET /api/standards/categories/list`

## 安装和启动

### 1. 更新数据库
```bash
# 使用新的数据库结构
mysql -u root -p < database/schema_v2.sql
```

### 2. 安装后端依赖
```bash
cd backend
npm install
# 会自动安装 multer
```

### 3. 启动后端服务
```bash
# 使用新的服务器文件
node server_v2.js

# 或
npm run dev
# 需要先修改 package.json 中的 start 脚本
```

### 4. 启动前端服务
```bash
cd frontend
npm install
npm run dev
```

## 使用指南

### 1. 上传抽样检查公告
1. 进入"公告管理"页面
2. 点击"上传公告"按钮
3. 填写公告信息：
   - 公告标题
   - 公告编号
   - 发布日期
   - 公告内容
4. 系统自动提取关键信息
5. 可选上传附件
6. 点击"上传并发布"

### 2. 查看企业检查情况
1. 进入"企业管理"页面
2. 筛选企业：
   - 输入企业名称、品牌、省份
   - 开启"只看不合格"开关
3. 点击企业行查看详情
4. 详情页展示：
   - 企业基本信息
   - 检查统计（被检次数、合格率等）
   - 检查历史记录

### 3. 统计分析
1. 进入"统计分析"页面
2. 查看不合格企业列表（按不合格次数排序）
3. 查看不合格次数TOP10图表
4. 查看省份分布饼图
5. 点击企业查看详情

## 自动提取功能说明

系统会从公告内容中自动提取以下信息：

### 检验单位
匹配模式：`经(.+?)等?(单位|所|中心|院)检验`
示例：
```
经贵州省食品药品检验所等单位检验
↓ 提取：贵州省食品药品检验所
```

### 抽检批次
匹配模式：`(\d+)批次.*?(不符合规定|不合格|有问题)`
示例：
```
产品标签标示名称为俏因子草本清肌泥膜粉等50批次化妆品不符合规定
↓ 提取：50
```

## 注意事项

1. **文件上传限制**
   - 支持格式：PDF、Word、Excel
   - 文件大小：最大10MB
   - 上传目录：`backend/uploads/announcements/`

2. **自动提取准确性**
   - 依赖公告内容的格式
   - 建议按照标准格式编写公告
   - 可手动修正提取结果

3. **企业关联**
   - 系统会根据生产企业名称自动关联企业
   - 未匹配的企业信息会保留在制造商字段中
   - 可手动创建企业信息进行关联

## 后续优化计划

- [ ] OCR识别：支持图片中的文字识别
- [ ] 批量导入：支持Excel批量导入检查数据
- [ ] 数据导出：支持导出各类报表
- [ ] 预警系统：不合格次数达到阈值自动预警
- [ ] 企业画像：多维度企业风险分析
- [ ] 移动端适配：优化手机端体验

## 问题反馈

如有问题或建议，请联系开发团队。
