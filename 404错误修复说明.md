# 404错误修复说明

## 问题描述

前端上传公告时出现 404 错误：
```
POST http://localhost:5173/api/announcements 404 (Not Found)
```

## 原因分析

1. 后端原有的 `announcements.js` 路由不支持文件上传（没有配置 multer）
2. 前端使用的是 `@/api/v2.js` 发送 FormData 请求
3. 后端需要支持 multipart/form-data 格式的文件上传

## 解决方案

### 1. 创建支持文件上传的路由

新文件：`backend/routes/announcements_upload.js`

特性：
- 使用 multer 中间件处理文件上传
- 支持的文件类型：PDF、Word、Excel
- 文件大小限制：10MB
- 自动提取关键信息（检验单位、批次数量）
- 文件存储路径：`backend/uploads/announcements/`

### 2. 更新后端配置

修改 `backend/server.js`：
```javascript
// 使用支持文件上传的路由
app.use('/api/announcements', require('./routes/announcements_upload'));
```

### 3. 创建上传目录

```bash
mkdir -p backend/uploads/announcements
```

### 4. 配置静态文件服务

已在 `server.js` 中添加：
```javascript
app.use('/uploads', express.static(path.join(__dirname, 'uploads')));
```

### 5. 修复前端下载路径

修改 `frontend/src/views/Announcements.vue`：
```javascript
const downloadAttachment = (row) => {
  if (row.attachment_path) {
    window.open(`http://localhost:3000${row.attachment_path}`, '_blank')
  }
}
```

## 文件上传流程

1. 前端选择文件并填写表单
2. 创建 FormData 对象，包含：
   - title: 公告标题
   - announcement_no: 公告编号
   - publish_date: 发布日期
   - content: 公告内容
   - attachment: 附件文件
3. 发送 POST 请求到 `/api/announcements`
4. 后端 multer 中间件处理文件上传
5. 后端自动提取关键信息
6. 将数据存入数据库
7. 返回成功响应

## 自动提取的信息

后端会从公告内容中自动提取：

1. **检验单位**
   - 正则：`/经(.+?)等?(单位|所|中心|院)检验/`
   - 示例："贵州省食品药品检验所"

2. **批次数量**
   - 正则：`/(\d+)批次.*?(不符合规定|不合格|有问题)/`
   - 示例：50批次 → 提取为 50

## 测试步骤

### 1. 启动后端

```bash
cd backend
npm run dev
```

### 2. 启动前端

```bash
cd frontend
npm run dev
```

### 3. 上传测试公告

1. 访问 http://localhost:5173
2. 点击"公告管理"
3. 点击"上传公告"
4. 填写表单：
   - 标题：国家药监局关于50批次不符合规定化妆品的通告（2026年第6号）
   - 编号：2026年第6号
   - 发布日期：2026-02-05
   - 内容：粘贴公告内容
   - 附件：选择一个文件（可选）
5. 点击"上传并发布"

### 4. 验证结果

- 公告应出现在列表中
- 自动提取的信息应正确显示
- 附件可以正常下载
- 公告详情页面应正常显示

## 数据库字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键 |
| title | VARCHAR(200) | 公告标题 |
| content | LONGTEXT | 公告内容 |
| announcement_no | VARCHAR(50) | 公告编号 |
| publish_date | DATE | 发布日期 |
| inspection_unit | VARCHAR(500) | 检验单位（自动提取） |
| inspection_count | INT | 抽检批次（自动提取） |
| attachment_path | VARCHAR(500) | 附件路径 |
| attachment_name | VARCHAR(200) | 附件原名 |
| status | ENUM | 状态：draft/published/archived |
| author_id | INT | 作者ID |
| view_count | INT | 浏览次数 |

## 注意事项

1. **文件大小限制**：单个文件不超过 10MB
2. **文件类型**：只支持 PDF、Word、Excel 文件
3. **文件命名**：使用时间戳 + 随机数避免冲突
4. **旧文件删除**：更新时会删除旧附件
5. **CORS配置**：已配置允许跨域请求

## 故障排查

### 问题1：上传时出现 "不支持的文件类型"

解决：只允许上传 .pdf、.doc、.docx、.xls、.xlsx 文件

### 问题2：上传时出现 "文件太大"

解决：文件大小不能超过 10MB

### 问题3：附件无法下载

解决：
1. 确认后端已启动
2. 确认 uploads 目录存在
3. 检查数据库中的 attachment_path 字段
4. 确认静态文件服务已配置

### 问题4：自动提取信息不准确

解决：
- 后端使用正则表达式提取，可能无法识别所有格式
- 可以手动编辑公告记录来修正提取的信息

## 完整的API端点

### 公告管理

- `GET /api/announcements` - 获取公告列表
- `GET /api/announcements/:id` - 获取公告详情
- `POST /api/announcements` - 创建公告（支持文件上传）
- `PUT /api/announcements/:id` - 更新公告（支持文件上传）
- `DELETE /api/announcements/:id` - 删除公告
- `GET /api/announcements/:id/inspections` - 获取相关检查记录

### 文件下载

- `GET /uploads/announcements/:filename` - 下载附件

## 已修复的文件列表

1. ✅ `backend/routes/announcements_upload.js` - 新建
2. ✅ `backend/server.js` - 更新路由
3. ✅ `frontend/src/views/Announcements.vue` - 修复下载路径和分页语法
4. ✅ `backend/uploads/announcements/` - 创建目录

系统现在可以正常上传公告了！
