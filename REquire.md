# 《化妆品抽检数据管理与分析系统》需求文档

## 一、项目概述
### 1.1 项目背景
项目当前是一套 化妆品抽检与飞行检查资讯库，带 JSON 导入 staging → 确认发布 → 通告/明细/企业/不合格聚合 → 透视分析 的闭环；数据库以 通告与明细、两类检查、企业关联、不合格宽表 + 导入与倒溯 为核心。在已有的功能基础上，增加新功能需求

### 1.2 项目目标
- 实现化妆品抽检数据的多维度、组合式检索
- 记录每次成功组合式检索，可供下次检索选择
- 支持用户自定义字段生成表格，灵活配置展示内容
- 提供标准化导出功能，支持Excel/CSV格式文件导出
- 提供基础可视化分析，支持数据洞察与汇报材料生成

---

## 二、功能需求
### 2.1 数据检索模块
#### 2.1.1 检索条件
| 维度 | 控件类型 | 可选值/说明 |  
| :--- | :--- | :--- | 
| 时间范围 | 日期范围选择器 | 支持自定义起始/结束年份，如2025-2026 |  
| 省份 | 下拉多选框 | 支持全国31个省市自治区多选 | 
| 企业名称 | 模糊搜索框 | 支持关键词匹配，如“广州XX化妆品公司” |  
| 产品品类 | 下拉多选框 | 面膜、精华、面霜、防晒、洁面等 |  
| 不合格项目 | 下拉多选框 | 甲硝唑、防腐剂超标、重金属超标、标签问题等 |  
| 通告批次 | 下拉多选框 | 在已选择的时间范围内发布的通告 |  

#### 2.1.2 检索逻辑
- 条件间为`AND`关系，需同时满足所有勾选条件
- 单维度多选条件为`OR`关系，满足任一选项即可命中
- 支持空条件检索（不勾选条件时，默认查询全量数据）

#### 2.1.3 检索结果
- 支持分页展示，默认每页10条
- 初始展示全字段数据，字段名可自定义配置，支持字段缩略
- 支持表格内快速搜索，可对结果数据二次过滤

---

### 2.2 自定义表格模块
#### 2.2.1 字段选择器
- 支持全量字段勾选/反选，字段分组展示（企业信息、产品信息、抽检信息）
- 支持字段拖拽排序，调整表格列展示顺序
- 支持保存常用字段模板，如“企业问题汇总表”“面膜不合格明细表”，下次检索一键套用

#### 2.2.2 表格配置
- 支持列宽自适应，可手动调整列宽
- 支持点击列头排序（升序/降序）
- 支持字段别名修改，导出文件中可展示自定义列名

---

### 2.3 结果导出模块
#### 2.3.1 导出格式
- Excel格式（.xlsx）：支持导出完整表格，包含表头、数据、列顺序
- CSV格式：适合后续数据分析，字段以逗号分隔

#### 2.3.2 导出配置
- 支持选择导出范围：当前页/全部检索结果
- 支持自动命名导出文件，文件名格式为`[筛选条件描述]-[导出时间].xlsx`，如`2025-2026-甲硝唑不合格企业数据-20260418.xlsx`
- 导出文件需包含筛选条件说明，可选择是否附加筛选条件备注页

<!-- #### 2.3.3 导出权限
- 支持按用户角色配置导出权限，普通用户可导出Excel，管理员支持全量数据导出 -->

---

<!-- ### 2.4 可视化分析模块（可选扩展）
- 支持生成基础统计图表，如省份分布柱状图、不合格项目占比饼图
- 支持图表导出为图片格式（PNG/JPG），用于短视频/汇报材料制作
- 支持按省份/企业维度生成Top10问题排行条形图 -->

---

## 三、非功能需求
### 3.1 性能需求
- 单次检索响应时间≤2秒（数据量≤10万条时）
- 导出1万条数据文件耗时≤10秒
- 

### 3.2 兼容性需求
- 支持主流浏览器：Chrome、Edge、Firefox
- 前端适配PC端，分辨率≥1366×768
- 导出文件兼容Office 2016及以上版本

### 3.3 数据安全需求
- 敏感数据脱敏处理，导出文件不含企业联系方式等隐私信息
- 操作日志记录：记录用户检索条件、导出时间、文件名称
- 支持数据备份，定期自动备份抽检数据

---

## 四、用户场景
### 场景1：不合格项目企业检索
用户输入条件：时间2025-2026年、不合格项目“甲硝唑”
- 系统返回所有检出甲硝唑的企业数据
- 用户勾选“企业名称、省份、产品名称、批次号”字段生成表格
- 导出Excel文件，用于问题企业台账整理

### 场景2：区域企业抽检分析
用户输入条件：省份“广东省、上海市”、无其他条件
- 系统返回广东省所有抽检数据
- 用户勾选“企业名称、抽检次数、不合格项目”字段
- 导出数据并生成企业抽检次数Top10图表，用于区域监管分析

### 场景3：品类专项抽检分析
用户输入条件：时间2026年、产品品类“面膜”
- 系统返回2026年面膜类产品抽检数据
- 用户勾选“省份、不合格项目、不合格批次数量”字段
- 导出文件并生成省份分布柱状图，用于专项抽检报告

---


## 五、验收标准
1. 所有检索条件组合可正常查询，无报错
2. 自定义表格字段选择、排序功能正常，列名展示正确
3. 导出文件字段与表格一致，格式兼容Office软件
4. 10万条数据检索响应时间≤2秒，导出耗时≤10秒

