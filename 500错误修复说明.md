# 500错误修复说明

## 问题
前端访问Dashboard API时出现500错误，错误信息显示：
```
Table 'cosmetics_info.policies' doesn't exist
```

## 根本原因
backend/routes/dashboard.js 仍然使用v1版本的路由，引用了不存在的policies和standards表。

## 已完成的修复

### 1. 创建v2版本的Dashboard路由
**文件**: `backend/routes/dashboard_v2.js`
- 移除了对policies和standards表的引用
- 使用v2版本的表结构：
  - announcements (公告表)
  - inspections (抽样检查表)
  - companies (企业表)
  - supervisions (督查表)
  - inspection_details (检查详情表)

### 2. 更新后端服务器配置
**文件**: `backend/server_v2.js`
```javascript
// 修复前
app.use('/api/dashboard', require('./routes/dashboard'));

// 修复后
app.use('/api/dashboard', require('./routes/dashboard_v2'));
```

### 3. 更新前端Dashboard组件
**文件**: `frontend/src/views/Dashboard.vue`

**统计卡片更新**:
- 移除: 政策法规、标准规范卡片
- 添加: 公告管理、企业管理卡片

**图表更新**:
- 移除: 政策级别分布图
- 保留: 督查状态分布图、抽样检查合格率趋势图

**数据表格更新**:
- 添加: 最新公告、最新督查表格
- 更新: 最新检查表格

**新增功能**:
- 高频问题企业列表 (基于检查结果统计)

### 4. Dashboard API新增功能
```javascript
{
  announcements: { total, published, draft, total_inspection_batches, total_views },
  inspections: { total, total_samples, total_qualified, total_unqualified, avg_qualified_rate, total_views },
  companies: { total, manufacturers, distributors, sellers },
  supervisions: { total, ongoing, completed, pending, total_views },
  unqualified: { total_unqualified, affected_companies },
  latest: {
    announcements: [...],
    inspections: [...],
    supervisions: [...]
  },
  problemCompanies: [
    { id, name, brand, province, inspection_count, unqualified_count }
  ]
}
```

## 需要执行的操作

**重启后端服务器以应用新的dashboard_v2路由**

### 方法1: 使用重启脚本
```bash
f:/daoke/化妆品/restart_server.bat
```

### 方法2: 手动重启
1. 停止当前后端服务 (Ctrl+C 或 taskkill /F /IM node.exe)
2. 重新启动:
   ```bash
   cd f:/daoke/化妆品/backend
   node server_v2.js
   ```

### 方法3: 使用启动脚本
```bash
f:/daoke/化妆品/start_v2.bat
```

## 验证修复

### 测试API端点
```bash
curl http://localhost:3000/api/dashboard/stats
curl http://localhost:3000/api/dashboard/trends
```

### 测试前端页面
访问 http://localhost:5173/dashboard 应该能正常显示所有统计数据

## 技术细节

### v1 vs v2 表结构对比
**v1 (已删除)**:
- policies (政策表)
- standards (标准表)

**v2 (当前使用)**:
- announcements (公告表)
- inspections (抽样检查表)
- companies (企业表)
- supervisions (督查表)
- inspection_details (检查详情表)

### Dashboard查询示例
```sql
-- 公告统计
SELECT COUNT(*) as total,
       SUM(CASE WHEN status = 'published' THEN 1 ELSE 0 END) as published
FROM announcements;

-- 高频问题企业
SELECT c.id, c.name, c.brand, c.province,
       COUNT(DISTINCT id.inspection_id) as inspection_count,
       SUM(CASE WHEN id.inspection_result = 'unqualified' THEN 1 ELSE 0 END) as unqualified_count
FROM companies c
JOIN inspection_details id ON c.id = id.company_id
WHERE id.inspection_result = 'unqualified'
GROUP BY c.id, c.name, c.brand, c.province
HAVING unqualified_count > 0
ORDER BY unqualified_count DESC
LIMIT 10;
```

## 测试状态
✅ 数据库查询测试通过
✅ 所有表结构正确
✅ Dashboard组件更新完成
✅ API路由配置更新完成

⚠️ 需要重启后端服务器
