# 热更新使用说明

## 📖 概述

系统已配置完整的前后端热更新功能，代码修改后会自动重新加载，无需手动重启服务器。

## 🚀 快速启动

### Windows用户

双击运行 `start_hotreload.bat` 或在命令行执行：

```bash
start_hotreload.bat
```

### Mac/Linux用户

```bash
chmod +x start_hotreload.sh
./start_hotreload.sh
```

## 🔧 配置说明

### 后端热更新 (Nodemon)

- **配置文件**: `backend/nodemon.json`
- **监控目录**: `*.js`, `routes/`, `config/`
- **监控文件类型**: `.js`, `.json`
- **忽略文件**: `node_modules/`, `*.test.js`

工作流程：
1. 修改 `backend/` 目录下的任意 `.js` 文件
2. Nodemon 自动检测到文件变化
3. 自动重启服务器
4. 控制台显示重启日志

### 前端热更新 (Vite)

- **配置文件**: `frontend/vite.config.js`
- **监控目录**: `frontend/src/`
- **自动刷新**: 浏览器自动刷新显示最新代码

工作流程：
1. 修改 `frontend/src/` 目录下的任意文件
2. Vite HMR (Hot Module Replacement) 检测到变化
3. 通过WebSocket推送到浏览器
4. 页面自动刷新或组件热更新

## 💡 使用技巧

### 后端开发

```bash
# 启动后端（单独）
cd backend
npm run dev
```

修改后端文件后：
- Nodemon 自动重启
- 查看控制台日志确认重启成功
- 刷新前端页面测试新功能

### 前端开发

```bash
# 启动前端（单独）
cd frontend
npm run dev
```

修改前端文件后：
- 页面自动刷新
- 组件状态可能重置
- Vite提供极快的更新速度

## 📝 注意事项

1. **数据库修改**: 修改数据库结构（`schema.sql`）后，需要手动重新导入数据库
2. **环境变量**: 修改 `.env` 文件需要手动重启后端服务器
3. **依赖更新**: 安装新依赖后需要重启服务器
4. **新增文件**: 新增的路由文件需在 `server.js` 中注册

## 🎯 推荐工作流

1. 启动热更新服务：`start_hotreload.bat`
2. 修改前端代码，观察页面自动更新
3. 修改后端代码，观察控制台自动重启
4. 完成开发后，使用生产模式测试：
   ```bash
   # 后端
   cd backend && npm start
   # 前端
   cd frontend && npm run build && npm run preview
   ```

## ⚡ 性能优化

- 后端：Nodemon 只监控必要的文件，减少不必要的重启
- 前端：Vite 使用原生 ESM，构建速度比 Webpack 快 10-100 倍
- 开发体验：秒级热更新，大幅提升开发效率

## 🔍 故障排查

### 后端不自动重启
- 检查 `nodemon.json` 配置
- 确认文件在监控目录下
- 查看控制台错误信息

### 前端不自动刷新
- 检查浏览器控制台
- 确认 WebSocket 连接正常
- 尝试手动刷新页面

### 端口冲突
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <进程ID> /F

# Mac/Linux
lsof -ti:3000 | xargs kill -9
```
