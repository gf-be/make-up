# 404错误修复说明

## 问题
前端访问后端API时出现404错误。

## 已完成的修复
1. ✅ 修复了 `frontend/vite.config.js` 中的API代理配置
2. ✅ 移除了错误的 `rewrite` 规则，现在正确保留 `/api` 前缀
3. ✅ 验证后端服务器运行正常 (端口3000)
4. ✅ 验证API端点正常响应
5. ✅ 验证数据库连接和表结构正常

## 需要执行的操作
**重启前端服务以应用新的Vite配置**

### 方法1: 手动重启
1. 在运行前端的命令行窗口中按 `Ctrl+C` 停止前端服务
2. 重新运行: `npm run dev`

### 方法2: 使用启动脚本
1. 关闭所有当前运行的服务
2. 运行: `start_v2.bat`

## 验证修复
重启后，访问以下页面验证:
- http://localhost:5173/announcements (公告管理)
- http://localhost:5173/inspections (抽样检查)
- http://localhost:5173/companies (企业管理)

## 技术细节
**问题原因:**
- 之前的Vite配置有错误的rewrite规则：`rewrite: (path) => path.replace(/^\/api/, '')`
- 这导致 `/api/inspections` 被重写为 `/inspections`
- 但后端期望的路径是 `/api/inspections`

**修复内容:**
```javascript
// 修复前
proxy: {
  '/api': {
    target: 'http://localhost:3000',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api/, '')  // ❌ 错误
  }
}

// 修复后
proxy: {
  '/api': {
    target: 'http://localhost:3000',
    changeOrigin: true  // ✅ 正确
  }
}
```

## 测试命令
可以使用以下命令测试API是否正常:
```bash
curl http://localhost:3000/api/inspections
curl http://localhost:3000/api/announcements
curl http://localhost:3000/api/companies
```
