# 化妆品资讯系统 - 快速启动指南

## 前置准备

确保已安装以下软件：
- Node.js (v16 或更高版本)
- MySQL (v8.0 或更高版本)

## 第一步：配置数据库

### 1. 启动 MySQL 服务
```bash
# Windows
net start mysql80

# 或通过 MySQL Workbench 启动
```

### 2. 执行数据库脚本
```bash
# 方式一：命令行
mysql -u root -p < database/schema.sql

# 方式二：MySQL Workbench
# 打开 MySQL Workbench
# 连接到数据库服务器
# 执行 database/schema.sql 文件
```

## 第二步：启动后端服务

### 1. 进入后端目录
```bash
cd backend
```

### 2. 安装依赖
```bash
npm install
```

### 3. 配置数据库连接
```bash
# 复制环境变量模板
cp .env.example .env

# 编辑 .env 文件，根据实际情况修改数据库配置
# 使用记事本或代码编辑器打开 .env 文件
```

### 4. 启动后端服务
```bash
npm run dev
```

后端服务将在 http://localhost:3000 运行

## 第三步：启动前端服务

### 1. 新开一个终端窗口，进入前端目录
```bash
cd frontend
```

### 2. 安装依赖
```bash
npm install
```

### 3. 启动前端服务
```bash
npm run dev
```

前端服务将在 http://localhost:5173 运行，浏览器会自动打开

## 访问系统

打开浏览器访问：http://localhost:5173

## 常见问题

### 1. 数据库连接失败
- 检查 MySQL 服务是否启动
- 确认 .env 文件中的数据库配置正确
- 检查用户名和密码是否正确

### 2. 端口被占用
如果 3000 或 5173 端口被占用：
- 修改 backend/.env 中的 PORT 配置
- 修改 frontend/vite.config.js 中的 server.port 配置

### 3. 依赖安装失败
```bash
# 清除缓存后重新安装
npm cache clean --force
npm install
```

### 4. 权限问题（Linux/Mac）
```bash
sudo npm install
```

## 开发模式说明

### 后端开发模式
使用 `npm run dev` 启动，支持热重载，修改代码后自动重启服务

### 前端开发模式
使用 `npm run dev` 启动，支持热更新，修改代码后自动刷新浏览器

## 生产环境部署

### 后端部署
```bash
cd backend
npm install --production
npm start
```

### 前端部署
```bash
cd frontend
npm run build
# 将 dist 目录部署到 Web 服务器
```

## 测试数据

数据库脚本已包含默认分类数据，可通过后端 API 添加更多测试数据。

## 停止服务

在对应的终端窗口按 `Ctrl + C` 停止服务

## 技术支持

如遇到问题，请检查：
1. Node.js 和 MySQL 版本是否符合要求
2. 数据库是否正确创建
3. 端口是否被其他程序占用
4. 网络连接是否正常
