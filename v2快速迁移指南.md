# v2.0 快速迁移指南

## 迁移步骤

### 第一步：更新数据库

```bash
# 停止现有服务
# 备份现有数据库（可选）
mysqldump -u root -p cosmetics_info > backup_$(date +%Y%m%d).sql

# 创建新的数据库结构
mysql -u root -p < database/schema_v2.sql
```

### 第二步：更新后端

```bash
cd backend

# 安装新依赖（multer）
npm install multer@1.4.5-lts.1

# 如果之前已经安装过，可以跳过
npm install
```

### 第三步：创建上传目录

```bash
# 在backend目录下创建uploads目录
mkdir -p backend/uploads/announcements

# Windows手动创建：
# backend/uploads/announcements
```

### 第四步：使用新的服务器文件

修改 `backend/package.json`：
```json
{
  "main": "server_v2.js"
}
```

然后启动：
```bash
npm run dev
```

或者直接运行：
```bash
node server_v2.js
```

### 第五步：更新前端

```bash
cd frontend

# 备份现有文件（可选）
cp src/router/index.js src/router/index_v1.js.bak
cp src/App.vue src/App_v1.js.bak
cp src/api/index.js src/api/index_v1.js.bak

# 使用新文件
# 将 router/index_v2.js 替换为 router/index.js
# 将 App_v2.vue 替换为 App.vue
# 将 api/v2.js 合并到 api/index.js
```

### 第六步：启动前端

```bash
npm run dev
```

## 数据迁移（如果需要保留v1数据）

如果有重要的v1数据需要迁移，请执行以下SQL：

```sql
-- 迁移抽样检查数据到新结构
UPDATE inspections i
SET announcement_id = (
  SELECT a.id
  FROM announcements a
  WHERE a.title LIKE CONCAT('%', i.title, '%')
  LIMIT 1
)
WHERE announcement_id IS NULL;

-- 创建企业信息（从检查详情中提取）
INSERT INTO companies (name, brand, type, address)
SELECT DISTINCT
  manufacturer,
  brand,
  'manufacturer',
  CONCAT(province, city)
FROM inspection_details
WHERE manufacturer IS NOT NULL
AND manufacturer NOT IN (SELECT name FROM companies);

-- 关联企业
UPDATE inspection_details id
SET company_id = (
  SELECT c.id
  FROM companies c
  WHERE c.name LIKE CONCAT('%', id.manufacturer, '%')
  LIMIT 1
)
WHERE company_id IS NULL AND manufacturer IS NOT NULL;
```

## 验证迁移

### 后端验证
```bash
# 访问健康检查
curl http://localhost:3000/health

# 访问API文档
curl http://localhost:3000/

# 测试公告列表
curl http://localhost:3000/api/announcements

# 测试企业统计
curl http://localhost:3000/api/companies/stats/overview
```

### 前端验证
1. 打开浏览器访问 http://localhost:5173
2. 检查导航菜单是否正确显示：
   - 数据概览 ✓
   - 公告管理 ✓
   - 抽样检查 ✓
   - 企业管理 ✓
   - 统计分析 ✓
   - 督查结果 ✓
3. 检查页面是否正常加载

## 功能测试清单

### 公告管理
- [ ] 公告列表显示正常
- [ ] 公告详情查看正常
- [ ] 上传公告对话框打开正常
- [ ] 公告内容输入正常
- [ ] 自动提取信息显示正常
- [ ] 附件选择正常
- [ ] 公告上传成功
- [ ] 附件下载正常

### 抽样检查
- [ ] 检查列表显示正常
- [ ] 检查详情查看正常
- [ ] 企业关联显示正常
- [ ] 筛选功能正常

### 企业管理
- [ ] 企业列表显示正常
- [ ] 统计卡片显示正常
- [ ] 企业详情查看正常
- [ ] 检查历史显示正常
- [ ] 合格率图表显示正常
- [ ] 筛选功能正常

### 统计分析
- [ ] 不合格企业列表显示正常
- [ ] TOP10图表显示正常
- [ ] 省份分布图显示正常

## 回滚方案

如果迁移出现问题，可以回滚：

### 1. 恢复数据库
```bash
mysql -u root -p cosmetics_info < backup_YYYYMMDD.sql
```

### 2. 恢复后端
```bash
# 使用旧的服务器文件
node server.js
```

### 3. 恢复前端
```bash
# 恢复备份文件
cp src/router/index_v1.js.bak src/router/index.js
cp src/App_v1.js.bak src/App.vue
cp src/api/index_v1.js.bak src/api/index.js
```

## 常见问题

### Q: 上传文件失败？
A: 检查以下几点：
1. uploads目录是否有写入权限
2. 文件大小是否超过10MB
3. 文件格式是否正确
4. multer是否正确安装

### Q: 自动提取信息不准确？
A:
1. 检查公告内容格式是否标准
2. 手动编辑修正提取结果
3. 联系开发团队优化提取规则

### Q: 企业关联不上？
A:
1. 检查企业名称是否完全匹配
2. 手动创建企业信息
3. 使用模糊匹配功能

### Q: 图表不显示？
A:
1. 检查echarts是否正确安装
2. 检查数据格式是否正确
3. 查看浏览器控制台错误信息

## 技术支持

迁移过程中遇到问题：
1. 查看本文档的"常见问题"部分
2. 查看控制台错误日志
3. 参考"版本更新说明_v2.md"
4. 联系开发团队

## 迁移完成检查清单

- [ ] 数据库更新完成
- [ ] 后端依赖安装完成
- [ ] 上传目录创建完成
- [ ] 后端服务启动成功
- [ ] 前端文件更新完成
- [ ] 前端服务启动成功
- [ ] 所有页面功能测试通过
- [ ] 上传功能测试通过
- [ ] 图表显示正常
- [ ] 数据统计正确

全部完成后，v2.0迁移成功！
